estimated weather forecast of (currently Gießen, Germany) for the current and future 5 days.

location is set via coordinates and can be changed if desired coordinates are known

goal is to run this notebook once a day and after some weeks measure the history to check how the actual weather differed from the estimation, these values and trends should then be used to calculate a more precise score.

idk what to say anymore
just run it
